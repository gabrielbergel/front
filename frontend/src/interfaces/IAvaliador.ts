export interface IAvaliador {
  id: number;
  nome: string;
  login: string;
  senha: string;
}

export interface IAvaliadorFormData {
  id: number;
  nome: string;
  login: string;
  senha: string;
}
